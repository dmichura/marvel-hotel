<?php

class ContactModel implements Model {
    public function __construct(&$page)
    {
        // $page['data'] 
        // print_r($page);
        // $result = $page['db']->query("INSERT INTO `ticket`(`title`, `email`, `reason`, `created`) VALUES ('[value-2]','[value-3]','[value-4]', NOW())");
        // $page['data'] = array(
        //     "ticketAdded" => $result[0]
        // );
    }
}

?>